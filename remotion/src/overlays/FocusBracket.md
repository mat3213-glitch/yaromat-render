---
component: FocusBracket
type: overlay
approved: no
reviewed: 2026-06-14
author: claude
---

# FocusBracket — акцент-рамка «фокус» + световой свип

Угловые скобки видоискателя снапаются внутрь + быстрый световой свип + лёгкая пульсация.
Первый оверлей-хук, ядро движка (на нём отлаживался `tsx_overlay_job.py`).

## Статус: ПРОД-АПРУВ НЕ ДАН

- Технически рендерится и композитится, НО затронут **общим багом композита** (см. ниже):
  альфа ProRes 4444 теряется на ffmpeg `overlay`, хук кроет арт вместо наложения поверх.
- Эстетически прод-апрув от yaromat не получен (общий фидбэк 2026-06-14: оверлей-эффекты
  в текущем виде дёшевы для прода).

## Что нужно для апрува
Починить альфа-композит, повторно оценить эффект на тёмной базе → `approved: yes`.
